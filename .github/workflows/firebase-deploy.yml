name: Firebase Deploy

on:
  push:
    branches: [ main, dev ]  # main → production, dev → staging

jobs:
  deploy-frontend:
    name: Deploy Frontend to Firebase Hosting
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout repository
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup pnpm
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      # 3. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'pnpm'

      # 4. Install frontend dependencies
      - name: Install Frontend Dependencies
        run: pnpm install --frozen-lockfile
        working-directory: frontend

      # 5. Compile smart contracts (backend)
      - name: Compile Contracts
        run: pnpm hardhat compile
        working-directory: backend

      # 6. Build Nuxt 4 (frontend)
      - name: Build Nuxt 4
        run: pnpm build
        working-directory: frontend

      # 7. Install Firebase CLI globally
      - name: Install Firebase CLI
        run: npm install -g firebase-tools@latest

      # 8. Prepare Firebase Service Account in workspace
      - name: Prepare Firebase Service Account
        shell: bash
        env:
          FIREBASE_JSON_PATH: ${{ github.workspace }}/firebase-service-account.json
        run: |
          # Write service account JSON from secret to file
          printf "%s" "${{ secrets.FIREBASE_SERVICE_ACCOUNT }}" > "$FIREBASE_JSON_PATH"
          # Set environment variable for Firebase CLI
          export GOOGLE_APPLICATION_CREDENTIALS="$FIREBASE_JSON_PATH"

      # 9. Deploy to Firebase Hosting
      - name: Deploy to Firebase Hosting
        shell: bash
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/firebase-service-account.json
        run: |
          # Deploy to production if main branch
          if [[ "${GITHUB_REF}" == "refs/heads/main" ]]; then
            firebase deploy --only hosting --project zephyra-prod
          # Deploy to staging if dev branch
          elif [[ "${GITHUB_REF}" == "refs/heads/dev" ]]; then
            firebase deploy --only hosting --project zephyra-dev
          fi
